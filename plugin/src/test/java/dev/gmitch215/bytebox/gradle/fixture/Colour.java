package dev.gmitch215.bytebox.gradle.fixture;

/** Read and written by name, so no reflection over the constants survives into the module. */
public enum Colour {
	/** Red. */
	RED,
	/** Green. */
	GREEN
}
