# gradle
.gradle
build
.kotlin

# bun
node_modules
package-lock.json

# npm package
# npm drops a symlinked LICENSE from the tarball, so prepack copies the real one in
npm/LICENSE
npm/typedoc
npm/coverage
npm/dist

# the workerd lane Workers under test, bundled by tests/workers/setup.ts
npm/tests/workers/*.build.mjs

# a real compiled module is committed under npm/tests/fixtures as a test input

# the instrumented module and what it records, both built by the coverage lane
npm/tests/fixtures/core-coverage.wasm
npm/coverage
npm/tests/coverage/*.build.mjs

# the self-signed certificate the tls container generates on startup
docker/certs

# generated worker output
.wrangler
size/ledger/*.json
!size/ledger/.gitkeep
.DS_Store

# the samples build against the bytebox in this tree
samples/*/build
samples/build
